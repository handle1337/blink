# blink


## Requirements

- Conda
- PyTorch

## Setup

1. Create a conda env (it's important not to use your base conda env for this project)

### In your Conda Environment

2. Install pip
3. run `pip install -r requirements.txt`